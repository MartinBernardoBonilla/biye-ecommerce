{
  "name": "biye-frontend",
  "version": "1.0.0",
  "description": "Frontend Flutter Web utilities",
  "scripts": {
    "build:web": "flutter build web --release",
    "serve:web": "flutter run -d web-server"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv-cli": "^11.0.0",
    "wkhtmltopdf": "^0.4.0"
  }
}
